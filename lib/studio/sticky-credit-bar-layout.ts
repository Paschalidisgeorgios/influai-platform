/**
 * Layout tokens for the sticky credit bar — spacing only, no billing logic.
 */

/** Bottom padding for page sections below the studio shell (toolbox, etc.). */
export const STICKY_CREDIT_BAR_PAGE_RESERVE =
  "overflow-x-hidden pb-8 sm:pb-10";

/** Extra clearance so docked credit bar / browser chrome never covers toolbox cards. */
export const CREATOR_TOOLBOX_SECTION_RESERVE = "pb-6 sm:pb-8";
